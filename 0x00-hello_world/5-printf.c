#include <stdio.h>

/**
 * main - function that prints a string using printf
 * Return: set return to 0
*/

int main(void)
{
	char string1[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", string1);
	return (0);
}
